import { connect } from 'react-redux';


import Presenter      from './nav-modal-presenter';

const mapStateToProps = (state) => ({

});


const Container = connect(mapStateToProps)(Presenter);


export default Container;